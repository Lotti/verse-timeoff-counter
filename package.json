{
  "name": "verse-timeoff-counter",
  "version": "0.0.1",
  "description": "Add TimeOff counter capabilities to Verse Calendar ",
  "main": "js/main.js",
  "homepage": "./",
  "scripts": {
    "react": "react-scripts start",
    "react:build": "react-scripts build",
    "react:eject": "react-scripts eject",
    "build": "npm run react:build && web-ext build",
    "lint": "web-ext lint",
    "sign": "web-ext sign",
    "docs": "web-ext docs",
    "start": "web-ext run",
    "dev": "web-ext run --verbose --firefox=/Applications/Firefox.app/Contents/MacOS/firefox-bin --firefox-profile=dev --keep-profile-changes",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.ibm.com/valerio-riva/verse-timeoff-counter.git"
  },
  "author": "Valerio Riva",
  "license": "MIT",
  "devDependencies": {
    "web-ext": "^4.0.0"
  },
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.3.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
